<div class="container">
  <h1 class="title-dashboard">Welcome to your dashboard <%= current_user.username %></h1>
  <div class="row">
    <div class="booking col-xs-12 col-sm-6">
      <h1>Your bookings</h1>
      <div class="booking-container">
        <%# @bookings.each do |booking| %>
        <div class="booking-card-body"></div>
        <!-- A booking card contain a fetch title and user avatar -->
        <!-- We need to print calculated price -->
        <!-- We need to print date of rent -->
        <!-- We need to print date of booking -->
      </div>
    </div>
    <div class="board-manager col-xs-12 col-sm-6">
      <h1>Boards manager</h1>
      <div class="board-manager-container">
        <div class="board-manager-cards">
          <div class="board-manager-picture">Board photo</div>
          <div class="board-manager-status">Board status</div>
        </div>
      </div>
      <%= link_to "Add a new board", new_board_path %>
    </div>
  </div>
</div>


<!-- <div class="dashboard">
  <p>Email: @user.email</p>
  <p>Username: @user.username</p>
  <h1>Boards</h1>
  <%# @user.boards.each do |board|  %>
  <p> <%#= board.title %> </p>
  <%# end %>

  <h1>bookings</h1>
  <%# @user.bookings.each do |booking|  %>
  <p> Start date: <%#= booking.start_date %> </p>
  <p> End date: <%#= booking.end_date %> </p>
  <%# end %>
</div>
 -->
